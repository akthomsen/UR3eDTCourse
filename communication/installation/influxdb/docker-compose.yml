services:
  influxdb:
    container_name: influxdb-server
    image: influxdb
    build: ./
    ports:
      - 8086:8086
    networks:
      - shared-communication

networks:
  shared-communication:
    external: true