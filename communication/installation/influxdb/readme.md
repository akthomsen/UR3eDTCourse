# Setup

When setting up the database, use "ur3e" everywhere possible, and if this is not valid use repetitions of it, e.g. "ur3eur3e" until it is valid.

In the data_recorder service, please update the token using the one provided in the interface by InfluxDB (localhost:8086).